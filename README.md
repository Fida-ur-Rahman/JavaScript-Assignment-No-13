# JavaScript Functions

## 📌 Overview
This repository provides clear examples of **JavaScript functions**, including declarations, expressions, arrow functions, parameters, and return values. It's ideal for learning and practice.

## 💡 Example
```javascript
// Function declaration
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet("Fida")); // Hello, Fida!

// Arrow function
const add = (a, b) => a + b;
console.log(add(5, 3)); // 8
